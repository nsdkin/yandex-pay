import os

DB_USE_ARCADIA_RECIPE = os.environ.get('DB_USE_ARCADIA_RECIPE', 'True') == 'True'

DB_CONNECT_TIMEOUT = int(os.environ.get('SPRINT_DB_CONNECTION_TIMEOUT', 2))
DB_TIMEOUT = int(os.environ.get('SPRINT_DB_TIMEOUT', 60))
