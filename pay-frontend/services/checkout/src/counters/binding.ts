import { count } from './utils';

export const bindingLoading = count('binding_loading');
export const firstBindingUserInput = count('first_binding_user_input');
export const bindingCanSubmit = count('binding_can_submit');
